# tuples are not changable
# tuple can contain item with diffrent data types

tup = (1, 4)
print(tup)
print(len(tup))
print(tup[0])
print(tup[1])

# indexing is same as list

# slicing is also same as list
